{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "install": { "commands": ["npm install"] },
    "build": { "commands": ["npx prisma generate"] }
  },
  "deploy": {
    "startCommand": "npx prisma db push && node prisma/seed.js && node src/index.js"
  }
}
